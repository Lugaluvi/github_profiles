name: github_profiles
description: Um buscador de perfis do Github feito totalmente em Flutter.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  url_launcher: ^6.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  google_fonts: ^2.1.0

flutter:

  uses-material-design: true
